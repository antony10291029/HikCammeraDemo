
在启动Demo之前，请你先看一下程序的配置文件OpenSDK_Demo_Qt.ini，文件存放在可执行程序目录下。
1. 程序进行用户认证时，需要开发者申请的AppKey，所以你需要在配置文件中填写你在开发平台所申请到的AppKey。
注意：填写AppKey字段时，前后不要有其他字符。

2. isSync为1时表示参数同步返回，isSync为0时表示参数异步返回。接口调用细则请参看OpenSDK接口说明文档

3. timeOut是在isSync=1的情况下需要设置的，同步接口等待超时时间。